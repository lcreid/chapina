<!DOCTYPE html>
<html>
  <head>
    <%= render "head" %>
  </head>

  <body>
    <%#S= render "site_nav" %>
    <%= render "flash" %>

    <div class="container">
        <div class="row justify-content-center">
          <div class="col-sm-4">
            <%= yield %>
          </div>
        </div>
    </div>

    <%= render "footer" %>
  </body>
</html>
