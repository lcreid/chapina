app: plazachapina
accessory: backup
databases:
  - name: app
    adapter: postgres
    url: postgres://chapina@postgres:5432/chapina
    password:
      secret: POSTGRES_PASSWORD
paths:
  # - /storage
restic:
  repository: sftp:admin@fs-2:/NetBackup/restic
  password:
    secret: RESTIC_PASSWORD
  init_if_missing: true
backup:
  schedule: 1d
